import { createUseStyles } from "react-jss";

export const useFooterStyles = createUseStyles({
  root: {
    display : "flex",
    justifyContent:"center",
    backgroundColor : "#1addf6",
    fontSize : "12px",
    height : "10vh",
  },
});
